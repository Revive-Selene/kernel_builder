name: Building kernel

on: # [push]
#  release:
#    types: [published]
#  push:
#    branches:
#      - master
#    paths:
#      - '.config'
#  schedule:
#    - cron: 0 8 * * 5
# Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      COMPILER:
        description: 'Compiler to use'
        required: true
        default: 'shattered-Clang-15'
      KREPO:
        description: 'Kernel repo URL'
        required: true
        default: 'https://github.com/Revive-Selene/android_kernel_xiaomi_selene'
      KBRANCH:
        description: 'Kernel branch'
        required: true
        default: '4.14'
      KSU:
        description: 'Build with ReSukiSU? (yes = compile with KSU, leave blank = no KSU) — must match branch: yes requires 4.14-rssu, blank requires 4.14'
        required: false
        default: ''
      NOTES:
        description: 'Extra notes (printed in build log)'
        required: false
        default: ''
      VERBOSE:
        description: 'Enable verbose logging (leave blank to disable)'
        required: false
        default: ''
      ZREPO:
        description: 'Custom AnyKernel3 repo (leave blank = use default from env)'
        required: false
        default: ''
      ZBRANCH:
        description: 'Custom AnyKernel3 branch (leave blank = use default from env)'
        required: false
        default: ''
      CONTAINER:
        description: 'Build container image'
        required: true
        default: 'fedora:40'

env:
  REPO: ${{ github.event.inputs.KREPO }}
  RUN_NUM: ${{ github.run_number }}_${{ github.run_attempt }}
  KERNEL_BRANCH: ${{ github.event.inputs.KBRANCH }}
  PATCH_KSU: ${{ github.event.inputs.KSU }}
  NOTE: ${{ github.event.inputs.NOTES }}
  COMPILERS: ${{ github.event.inputs.COMPILER }}
  VERBOSE: ${{ github.event.inputs.VERBOSE }}
  zipper_repo: ${{ github.event.inputs.ZREPO }}
  zipper_branch: ${{ github.event.inputs.ZBRANCH }}
  TZ: Asia/Jakarta

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ${{ github.event.inputs.CONTAINER }}

    steps:
        - name: checkout
          uses: actions/checkout@master

        - name: record start time
          run: |
            echo WORKFLOW_START=$(date +"%s") >> $GITHUB_ENV

        - name: init build env
          run: |
            dnf group install development-tools -y
            dnf install llvm lld bc bison ca-certificates curl flex glibc-devel.i686 glibc-devel binutils-devel openssl python3 python2 zstd clang gcc-arm-linux-gnu dtc libxml2 libarchive openssl-devel perl tomsfastmath-devel wget xz -y
            ln -sf "/usr/bin/python3" /usr/bin/python
            git config --global user.name "Revive-Selene"
            git config --global user.email "revive-selene@github.com"

        - name: show host info
          run: |
            if [ ! -z "$VERBOSE" ]; then
              echo "Host: $(cat /etc/os-release | grep PRETTY_NAME | cut -d'=' -f2)"
              echo "Free disk space: $(df --sync -BM --output=avail / | grep -v Avail)"
            fi

        - name: print notes if exists
          run: |
            if [ ! -z "$NOTE" ]; then
              echo "Note: $NOTE"
            fi

        - name: validate KSU + branch combination
          run: |
            if [ "$PATCH_KSU" = "yes" ] && ! echo "$KERNEL_BRANCH" | grep -q "rssu"; then
              echo "❌ ERROR: KSU=yes requires a ReSukiSU-integrated branch (e.g. 4.14-rssu)."
              echo "   Your branch '$KERNEL_BRANCH' does not contain ReSukiSU hooks."
              echo "   Either set KSU=blank to build without KSU, or change branch to 4.14-rssu."
              exit 1
            fi
            if [ -z "$PATCH_KSU" ] && echo "$KERNEL_BRANCH" | grep -q "rssu"; then
              echo "⚠️  WARNING: Branch '$KERNEL_BRANCH' has ReSukiSU integrated but KSU is blank."
              echo "   The kernel will still compile but ReSukiSU version string will NOT be applied."
            fi

        - name: clone kernel source
          run: |
            echo "Cloning kernel from: $REPO (branch: $KERNEL_BRANCH)"
            git clone --depth=1 --single-branch --recurse-submodules -j4 $REPO -b $KERNEL_BRANCH kernel

        - name: print kernel info
          run: |
            cd kernel
            source ../env
            echo "Kernel name: ${kernel_name}"
            echo "Kernel ver:  ${kernel_ver}"
            echo "Head commit: ${kernel_head}"
            echo "Defconfig:   ${defconfig}"
            echo "KSU mode:    ${PATCH_KSU:-none}"

        - name: apply ReSukiSU (KSU=yes)
          if: env.PATCH_KSU == 'yes'
          run: |
            cd kernel
            bash ../ksu/applyPatches.sh

        - name: run compilation
          run: |
            cd kernel
            bash ../build.sh "$COMPILERS"

        - name: upload artifacts
          uses: actions/upload-artifact@v4
          if: always()
          with:
            name: kernel-${{ github.run_number }}-${{ github.run_attempt }}
            path: |
              kernel/*.zip
              kernel/*.log
            if-no-files-found: warn
            retention-days: 7

        - name: print build result
          if: always()
          run: |
            WORKFLOW_END=$(date +"%s")
            WDIFF=$((WORKFLOW_END - WORKFLOW_START))
            if [ "${{ job.status }}" = "success" ]; then
              echo "✅ Build succeeded in $((WDIFF / 60))m $((WDIFF % 60))s"
            else
              echo "❌ Build failed in $((WDIFF / 60))m $((WDIFF % 60))s"
            fi
